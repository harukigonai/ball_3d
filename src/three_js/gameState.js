export class GameState {
    ballMap
    playerMap

    constructor() {}

    setBallMap(ballMap) {
        this.ballMap = ballMap
    }

    setPlayerMap(playerMap) {
        this.playerMap = playerMap
    }
}
